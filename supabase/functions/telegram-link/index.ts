import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";
const TELEGRAM_BOT_TOKEN=Deno.env.get("TELEGRAM_BOT_TOKEN")??"",SUPABASE_URL=Deno.env.get("SUPABASE_URL")??"",SUPABASE_ANON_KEY=Deno.env.get("SUPABASE_ANON_KEY")??"",SUPABASE_SERVICE_ROLE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"",MAX_AGE_SECONDS=3600;
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{"Content-Type":"application/json"}});
async function hmac(key:ArrayBuffer|Uint8Array,message:string){const cryptoKey=await crypto.subtle.importKey("raw",key,{name:"HMAC",hash:"SHA-256"},false,["sign"]);return new Uint8Array(await crypto.subtle.sign("HMAC",cryptoKey,new TextEncoder().encode(message)));}
function equal(a:Uint8Array,b:Uint8Array){if(a.length!==b.length)return false;let r=0;for(let i=0;i<a.length;i++)r|=a[i]^b[i];return r===0;}
async function verify(initData:string){if(!TELEGRAM_BOT_TOKEN||!initData)throw new Error("Telegram initData is required.");const params=new URLSearchParams(initData),hash=params.get("hash"),authDateRaw=params.get("auth_date");if(!hash||!authDateRaw)throw new Error("Invalid Telegram initData.");const authDate=Number(authDateRaw);if(!Number.isFinite(authDate)||Math.abs(Math.floor(Date.now()/1000)-authDate)>MAX_AGE_SECONDS)throw new Error("Telegram initData has expired.");params.delete("hash");const data=Array.from(params.entries()).sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>k+"="+v).join("\n");const secret=await hmac(new TextEncoder().encode("WebAppData"),TELEGRAM_BOT_TOKEN),calculated=await hmac(secret,data),provided=new Uint8Array(hash.match(/.{1,2}/g)?.map((b)=>parseInt(b,16))??[]);if(!equal(calculated,provided))throw new Error("Telegram initData signature is invalid.");return Object.fromEntries(params.entries());}
Deno.serve(async(req:Request)=>{if(req.method!=="POST")return json({error:"Method not allowed"},405);try{const auth=req.headers.get("Authorization")??"";if(!auth.startsWith("Bearer "))return json({error:"Authorization required"},401);if(!SUPABASE_URL||!SUPABASE_ANON_KEY||!SUPABASE_SERVICE_ROLE_KEY)return json({error:"Supabase function secrets are not configured."},500);const client=createClient(SUPABASE_URL,SUPABASE_ANON_KEY,{global:{headers:{Authorization:auth}},auth:{persistSession:false,autoRefreshToken:false}}),{data:{user},error}=await client.auth.getUser();if(error||!user)return json({error:"Invalid authentication session."},401);const body=await req.json().catch(()=>({})),verified=await verify(typeof body?.initData==="string"?body.initData:""),telegramUser=JSON.parse(verified.user??"{}");if(!Number.isSafeInteger(telegramUser.id))throw new Error("Invalid Telegram user id.");const admin=createClient(SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}}),{data:existing,error:lookupError}=await admin.from("telegram_identities").select("user_id").eq("telegram_user_id",telegramUser.id).maybeSingle();if(lookupError)throw lookupError;if(existing&&existing.user_id!==user.id)return json({error:"This Telegram account is already linked to another AERQVON account."},409);const {data:identity,error:upsertError}=await admin.from("telegram_identities").upsert({user_id:user.id,telegram_user_id:telegramUser.id,telegram_username:telegramUser.username??null,first_name:telegramUser.first_name??null,last_name:telegramUser.last_name??null,language_code:telegramUser.language_code??null,updated_at:new Date().toISOString()},{onConflict:"telegram_user_id"}).select("id,user_id,telegram_user_id,telegram_username,first_name,last_name,language_code,linked_at,updated_at").single();if(upsertError)throw upsertError;return json({linked:true,identity});}catch(error){console.error("telegram-link error",error);return json({error:error instanceof Error?error.message:"Telegram linking failed."},400);}});
