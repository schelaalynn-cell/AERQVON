import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const TELEGRAM_BOT_TOKEN=Deno.env.get("TELEGRAM_BOT_TOKEN"),SUPABASE_URL=Deno.env.get("SUPABASE_URL"),SUPABASE_SERVICE_ROLE_KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{"Content-Type":"application/json"}});
async function telegram(method:string,body:Record<string,unknown>){if(!TELEGRAM_BOT_TOKEN)throw new Error("TELEGRAM_BOT_TOKEN is not configured");const response=await fetch("https://api.telegram.org/bot"+TELEGRAM_BOT_TOKEN+"/"+method,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!response.ok)throw new Error("Telegram API returned "+response.status);return response.json();}
Deno.serve(async(req:Request)=>{if(req.method!=="POST")return json({error:"method_not_allowed"},405);if(!SUPABASE_URL||!SUPABASE_SERVICE_ROLE_KEY)return json({error:"server_not_configured"},500);try{const update=await req.json(),message=update?.message,from=message?.from;if(!message||!from?.id)return json({ok:true,ignored:true});const telegramUserId=Number(from.id),firstName=typeof from.first_name==="string"?from.first_name:null;const supabase=createClient(SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY);const {data:identity}=await supabase.from("telegram_identities").select("user_id").eq("telegram_user_id",telegramUserId).maybeSingle();let aqvUserId:string|null=null;if(identity?.user_id){const {data:profile}=await supabase.from("aqv_user_profiles").select("aqv_user_id").eq("user_id",identity.user_id).maybeSingle();aqvUserId=profile?.aqv_user_id??null;}const command=typeof message.text==="string"?message.text.trim().split(/\s+/)[0].toLowerCase():"";if(command==="/start")await telegram("sendMessage",{chat_id:message.chat.id,text:identity?.user_id?"Welcome back to AERQVON, "+(firstName??"there")+"."+ (aqvUserId?"\nAERQVON User ID: "+aqvUserId:""):"Welcome to AERQVON, "+(firstName??"there")+". Your Telegram account is not linked yet."});else if(command==="/help")await telegram("sendMessage",{chat_id:message.chat.id,text:"/start — account status\n/help — available commands\n/status — AERQVON account link status"});else if(command==="/status")await telegram("sendMessage",{chat_id:message.chat.id,text:identity?.user_id?"Your Telegram account is linked to AERQVON.":"Your Telegram account is not linked to AERQVON."});return json({ok:true});}catch(error){console.error("telegram-webhook error",error);return json({error:"internal_error"},500);}});
